apiVersion: v1
kind: Service
metadata:
  name: {{ include "safe-triage-demo.fullname" . }}
  labels:
    {{- include "safe-triage-demo.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "safe-triage-demo.selectorLabels" . | nindent 4 }}
